VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_API_URL=

# Paddle.js client-side token — Paddle Dashboard -> Developer Tools ->
# Authentication -> Client-side tokens. Safe to expose (not a secret).
VITE_PADDLE_CLIENT_TOKEN=
VITE_PADDLE_ENVIRONMENT=sandbox
