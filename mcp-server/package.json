{
  "name": "@lazyrelay/mcp-server",
  "version": "0.1.0",
  "private": false,
  "description": "MCP server for LazyRelay — schedule posts, check analytics, and read mentions from any MCP-compatible AI agent using your own LazyRelay API key.",
  "type": "module",
  "bin": {
    "lazyrelay-mcp": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.16.0",
    "typescript": "^5.5.0"
  }
}
