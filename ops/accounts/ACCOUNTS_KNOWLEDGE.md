# LazyRelay Accounts — Operating Knowledge

Living document for the Accounts Operator/Auditor. Read every run before doing anything else. Update whenever a genuinely new account-state scenario is handled that isn't covered here yet — same discipline as `support/SUPPORT_KNOWLEDGE.md`.

## Domain boundary (decided 2026-07-22)

**Accounts owns the customer-account state machine.** **Billing owns pure money events.** The split: Billing detects a payment event (success/failure) and reports it; Accounts is the one that actually reacts by changing account state and what that means product-side (e.g. pausing scheduled posts). Accounts never initiates a charge, refund, or subscription-tier change — that's Billing's job.

## Plain-language state mapping (grounded in the real schema — no invented states)

The real state machine lives in `subscriptions.status`: `trialing` / `active` / `past_due` / `cancelled`. There is no literal "blocked" value anywhere in the schema. Mapping onto the buckets discussed with the user:

| Plain-language bucket | Real DB condition |
|---|---|
| New | `subscriptions.status = 'trialing'`, or no subscription row exists yet at all (`new_no_subscription`) |
| Unpaid / blocked | `subscriptions.status = 'past_due'` |
| Existing | `subscriptions.status = 'active'` |
| Cancelled | `subscriptions.status = 'cancelled'` |

## Downgrade policy (pause, never delete)

Documented policy from `support/SUPPORT_KNOWLEDGE.md`: "accounts beyond the new limit get paused, never auto-deleted. Let the customer choose which stay active." As of migration `0013_social_accounts_paused.sql` (2026-07-23), this is fully enforced, not just planned: `accounts_ops.js::planDowngradePause()` picks which accounts would be paused (oldest-connected-first stays active), `enforceDowngradePause()` actually sets `social_accounts.paused_at` on the rest, and `unpauseAccounts()` reverses it on re-upgrade or reconnection. `backend/src/scheduler.ts`'s `processPost()` checks `paused_at` before every post attempt and fails immediately (no retry, no circuit-breaker impact) if the account is paused — a paused account's tokens and connection stay fully intact, it just can't post until unpaused. **2026-07-28: wired.** `backend/src/billing/sync.ts::syncSubscriptionFromWebhook()` now calls `unpausePausedAccountsUpToLimit()` whenever a webhook brings a subscription to `status: "active"` — it unpauses oldest-paused-first up to the new tier's `ACCOUNT_LIMITS` cap. This is a TS reimplementation living in the backend (not a call into this JS module directly, since backend/ compiles as ESM and ops/ is CommonJS) — keep the pause-side policy here and the unpause-side policy in sync.ts in sync if either one's limit/ordering logic changes.

## Stuck onboarding

Signed up 7+ days ago with zero connected `social_accounts` (excluding disconnected ones) — a real candidate for a nudge. **Send-authority granted 2026-08-05** (see `03 - LazyRelay/Support/feedback-email-agent-draft-and-hold.md` in the vault): `lazyrelay-accounts-ops-daily` now sends Template 11 directly from `support@lazyrelay.com` via `support/email-agent/imap-tool.js`'s `send-mail`, confirming `savedToSent: true` before counting it sent. Draft-and-hold is retired for this path — but the escalation half of that discipline stands: anything ambiguous or unusual is skipped and flagged for Werner, never sent blind.

## Review requests (built 2026-07-22)

There's no public reviews/testimonials section on the site yet — real customers don't exist yet, and displaying fake or placeholder reviews would undercut the Proof-of-Publish trust positioning. But *collecting* reviews doesn't need a public section, and doesn't need to wait: `accounts_ops.js::findReviewRequestCandidates()` finds accounts with a real milestone (5+ posts confirmed actually live via `post_results.verified_live`, not just "sent" — the same trust signal the product itself is built on) and surfaces them as candidates for a review-request email. Excludes cancelled accounts. Dedup via a `StateStore` at `ops/accounts/state/review_requests.json` — `markReviewRequested(accountId)` is only called once a draft is actually saved, so a dry-run that just lists candidates never burns the mark, and no account is ever asked twice.

**Send-authority granted 2026-08-05**, same as stuck-onboarding: the scheduled task now sends Template 12 directly from `hello@lazyrelay.com` via `send-mail`, confirms `savedToSent: true`, then immediately calls `markReviewRequested(accountId)`. `markReviewRequested()` is still only called once a send is *confirmed*, so a run that merely lists candidates never burns the mark. The reply (an actual quote/rating) lands back in the inbox — there's no separate storage table for it yet, since with zero reviews collected so far there's nothing to justify building a table before there's real content to put in it. Once a handful of real quotes exist, that's the trigger to (a) decide where to store them and (b) build the public-facing Reviews section — not before.

## Test/internal accounts dominate both candidate lists (found on the first live send-authority run, 2026-08-05)

Neither `findStuckOnboardingAccounts()` nor `findReviewRequestCandidates()` filters out non-customer rows, and in the current pre-launch database almost every candidate is one. First live run: 34 stuck-onboarding candidates, of which **31** were `@lazyrelay.invalid` and 2 were Werner's own accounts (`werner@lazyrelay.com`, `goss.werner.1@gmail.com`) — exactly **1** was a genuine external signup. Review requests: 2 candidates, **0** genuine.

Exclusion rules — **now enforced in code**, not manual triage. `ops/shared/internalTestAccounts.js::isInternalTestAccount()` (added 2026-08-05) is called by both `findStuckOnboardingAccounts()` and `findReviewRequestCandidates()`, so internal rows never reach a candidate list at all:
- **Domain patterns:** `@lazyrelay.invalid` (the `.invalid` TLD is reserved by RFC 2606 and undeliverable — every sandbox/e2e/stress/migration/ui-check fixture uses it), `@lazyrelay.com`, `@lazydownloader.co.za`.
- **Exact addresses:** `goss.werner.1@gmail.com`, `jacobsluzaan@gmail.com` — real consumer domains a pattern can't cover.

`shop@lazydownloader.co.za` is The Lazy Download dogfooding LazyRelay (active subscription, 9 verified-live posts). It trips the review-request milestone legitimately but is Werner's own business, so it's excluded by domain pattern rather than asked for a review.

**2026-08-06 run:** both candidate lists came back **empty**. 58 accounts exist; 54 are `@lazyrelay.invalid` and the remaining 4 are Werner's/Luzaan's own addresses — **zero genuine external customers so far**. The "1 genuine external signup" recorded on 2026-08-05 was `jacobsluzaan@gmail.com`, reclassified as internal the same day when the exact-match list was confirmed with Werner. Empty lists are therefore the *correct* result right now, not a broken query — expect them to stay empty until real signups arrive. If a run ever reports zero when real customers do exist, suspect the filter, not the data.

**2026-08-14 run:** both lists empty again. 62 accounts total (60 non-cancelled — the auditor filters `.is("cancelled_at", null)`, so a `checked` count 2 below the table total is expected, not drift). One row now passes `isInternalTestAccount()` as "genuine external": **`lazyrelay+reviewer@gmail.com`** (created 2026-08-13). The `lazyrelay+` plus-tag and the `reviewer` label make it look like an internal/app-review fixture rather than a customer, but it matches no exclusion pattern. It didn't reach either candidate list anyway (1 day old, so nowhere near the 7-day stuck-onboarding threshold, and no verified-live posts), so nothing was sent — flagged for Werner rather than filtered on a guess. **Decision needed:** if it's an internal reviewer account, add `lazyrelay+` (or the exact address) to `ops/shared/internalTestAccounts.js`; if it's real, leave it and expect it on the stuck-onboarding list from 2026-08-20 onward.

**2026-08-15 run — the reviewer account is IDENTIFIED, and it is internal.** Population identical (62 / 60 non-cancelled / 1 genuine-external), both lists empty, nothing sent. The 08-14 "decision needed" is answered by evidence in the vault's `Active Priorities.md`: **`lazyrelay+reviewer@gmail.com` is the test account Werner created for Google's YouTube/OAuth verification reviewers** (created 2026-08-13, credentials in the password manager, cited in the T&S reply sent to Google that day — see `03 - LazyRelay/Platforms/project-youtube-google-verification-2026-08-03.md`). It is a reviewer fixture, not a customer.

Consequence if nothing changes: on **2026-08-20** it turns 7 days old with zero connected social accounts, lands on the stuck-onboarding list, and this task sends Google's reviewer a "how's onboarding going?" nudge from `support@lazyrelay.com` — during an open verification review. **Required fix (needs Werner's confirmation — source-code edit, out of this task's send-only authority):** add `lazyrelay+` (plus-tag prefix on the `lazyrelay@gmail.com` mailbox, covers any future reviewer fixture) or the exact address to `ops/shared/internalTestAccounts.js`. Note the account is also slated for deletion once Google responds, which would close it the other way — but deletion is not scheduled and the 08-20 deadline is, so the filter is the safe move.

**RESOLVED — verified in code 2026-08-16.** `ops/shared/internalTestAccounts.js`'s `INTERNAL_TEST_EMAILS_EXACT` now carries `lazyrelay+reviewer@gmail.com` (commented "Google OAuth reviewer test account, confirmed 2026-08-15"). The exact address was used rather than a `lazyrelay+` prefix pattern, so a *future* reviewer fixture on a different plus-tag would not be covered — add it explicitly if one is created. The account row still exists (created 2026-08-13, not cancelled), so the filter is what's holding the 08-20 deadline, not deletion. 2026-08-16 runs: first run 61 accounts checked; second run 60 checked (62 total, 2 cancelled — one account cancelled between the two runs). Both candidate lists empty on both, nothing sent. Second run also confirms the filter is live-effective end-to-end: genuine-external count is now **0** (was 1 while the reviewer address was unfiltered), so the 08-20 nudge-to-Google's-reviewer risk is closed. **2026-08-19 (the day before the deadline):** filter re-verified present in `INTERNAL_TEST_EMAILS_EXACT`; auditor checked 61, 0 flagged; both candidate lists empty. The reviewer account crosses 7 days old on 08-20 and the filter is the only thing holding it — if that line is ever removed while the row still exists, the nudge fires. **2026-08-20 — the deadline day passed clean.** Filter re-verified present; auditor checked 62, 0 flagged; stuck-onboarding list **empty** on the exact day the reviewer account became eligible for it, which is the proof the filter works at the moment it mattered, not just in principle. No nudge was sent to Google's reviewer. This closes the open risk — the only remaining exposure is a *future* reviewer fixture on a different plus-tag, which the exact-address entry would not cover. (Confirmed twice on 08-20: the read-only ops digest ran first, then `lazyrelay-accounts-ops-daily` — the task that actually holds send authority — ran independently later the same day and also got 62 checked / 0 flagged / both lists empty. So the deadline held on the sending path, not just the reporting one.)

**2026-08-21 — the plus-tag gap predicted above has now actually happened, on Werner's own address.** 65 rows, 63 non-cancelled, auditor 0 flagged, both candidate lists empty, nothing sent. But a new account created today at 08:27 UTC — **`goss.werner.1+lrtest1@gmail.com`** — passes `isInternalTestAccount()` as genuine-external. `goss.werner.1@gmail.com` is in `INTERNAL_TEST_EMAILS_EXACT`, but matching is exact-string on the lowercased address with no plus-tag stripping, so `+lrtest1` slips through. The `lrtest1` label and the fact that it's Werner's own mailbox make it a test signup, not a customer. **Deadline: 2026-08-28** — at 7 days old with zero connected social accounts it lands on the stuck-onboarding list and this task emails Werner's own inbox a "how's onboarding going?" nudge. **Required fix (source-code edit, outside this task's send-only authority — needs Werner's confirmation):** normalize plus-tags in `ops/shared/internalTestAccounts.js::isInternalTestAccount()` (strip `+…` before the `@` prior to the exact-set lookup), which fixes this class of gap permanently and also covers any future `lazyrelay+…` reviewer fixture — strictly better than adding another one-off exact address.

**2026-08-22 — the test-data population was purged. `accounts` went 65 → 1 overnight.** The sole surviving row is `shop@lazydownloader.co.za` (created 2026-07-27, `active`, tier `enterprise`) — The Lazy Download dogfooding LazyRelay. Every `@lazyrelay.invalid` fixture, both Werner's/Luzaan's addresses, the `+lrtest1` test signup and the `lazyrelay+reviewer@gmail.com` Google-reviewer fixture are gone.

Verified as a clean deletion, not a broken query, a wrong-database connection, or orphaning — `auth.users` is **also** down to 1, so the delete happened at the auth level and cascaded correctly:

| Table | Rows | Orphaned |
|---|---|---|
| `auth.users` | 1 | — |
| `accounts` | 1 | — |
| `subscriptions` | 1 | 0 |
| `social_accounts` | 14 | 0 |
| `post_results` | 49 | 0 |
| `billing_records` | 12 | **12** |

`billing_records`' 12 orphaned rows are **correct, not corruption** — the SARS bookkeeping trail is deliberately retained past account deletion (IPE Projects (Pty) Ltd needs the paperwork whether or not the customer account still exists). Don't "fix" this; don't report it as a cascade bug.

Consequences for future runs:
- **`checked: 1` is the new expected baseline.** A one-row audit is now the healthy result, not evidence of a broken filter. The 2026-08-06 note above ("if a run reports zero when real customers do exist, suspect the filter, not the data") still holds, but the population it was written against no longer exists.
- **The plus-tag gap flagged on 2026-08-21 was fixed the same day (commit `888b85f`).** `stripGmailAlias()` is implemented in `ops/shared/internalTestAccounts.js` and `isInternalTestAccount()` checks both the raw and de-aliased form — 8/8 real test cases pass, including the negative case that a single literal address like `lazyrelay+reviewer@gmail.com` isn't accidentally widened to bare `lazyrelay@gmail.com`. A plus-tagged signup on a known internal mailbox no longer slips through.
- **`lazyrelay+reviewer@gmail.com` was deleted while its exact-address entry remains in `INTERNAL_TEST_EMAILS_EXACT`.** Harmless (the filter just never matches now). Google's verification review is closed — approved 2026-08-14, both scopes granted by name (confirmed off the real approval mail at `accounts@`, uid 117) — so the account's deletion on 08-17 was correctly timed and independently reconfirmed gone 08-18.
- The first genuine external signup is now unambiguous: any second row appearing in `accounts` is either a real customer or a new fixture, with no 60-row haystack around it.

The 2026-08-22 purge was Werner's own instruction, given directly on 2026-08-21 ("we only use the one the rest we can cancel") — paused and re-confirmed mid-run when the scope turned out to be 62 accounts rather than 2. No confirmation is outstanding.

**Confirmed on the sending path the same day.** `lazyrelay-accounts-ops-daily` — the task that actually holds send authority — ran independently after the read-only digest and got the same result: self-test `overallPass: true`, auditor `checked: 1 / flaggedCount: 0`, both candidate lists empty, nothing sent. So `checked: 1` is the baseline for the sending path too, not just the reporting one. **Baseline still holding as of 2026-08-30, confirmed on the sending path** (same figures, nothing sent; on 08-30 the read-only digest ran first and `lazyrelay-accounts-ops-daily` — the task holding send authority — confirmed it independently later the same day: self-test `overallPass: true`, auditor `checked: 1 / flaggedCount: 0`, both candidate lists empty) — no second row has appeared, so the first genuine external signup is still ahead of us. Don't add a new paragraph per quiet run; just carry this date forward.

~~**2026-08-30 — new latent gap: the dogfooding account's login email changed, and the new address isn't excluded.**~~ **FIXED 2026-08-30, same day (commit `c658ba3`).** On 2026-08-29 Werner changed the sole surviving account's *login* email from `shop@lazydownloader.co.za` to `lazyrelay@gmail.com` (see the 2026-08-29 daily note and `reference-infra-quick-facts`). `accounts.email` still reads `shop@lazydownloader.co.za` (the drift itself wasn't touched — a real `accounts` row edit is a separate decision), but `lazyrelay@gmail.com` is now in `INTERNAL_TEST_EMAILS_EXACT` alongside it, so `isInternalTestAccount()` returns `true` for whichever of the two fields ends up holding the live value. Verified directly: `isInternalTestAccount('lazyrelay@gmail.com') === true`, and the three pre-existing cases (`lazyrelay+reviewer@gmail.com`, `shop@lazydownloader.co.za`, a real external address) all still resolve correctly — no regression.

Why it mattered: this account is `active`, tier `enterprise`, with 9+ verified-live posts — it trips the review-request milestone legitimately, and a future backfill/re-sync of `accounts.email` to the auth email would have made this task email Werner's own inbox a review request from `hello@lazyrelay.com`. That risk is now closed regardless of which field the filter reads. The related **Seats-list display drift** (still showing the old owner email) is a separate, cosmetic issue, not fixed by this — still open, see [[Active Priorities]].

**2026-08-31 — the baseline moved: `accounts` is 2, not 1, and the new row is an unfiltered test fixture with a hard deadline.** Auditor `checked: 2 / flaggedCount: 0`; both candidate lists still empty; nothing sent. The second row is **`lazyrelay-disposable-onboarding-test@example.com`** (created 2026-08-30 11:09 UTC) — the disposable account created via the Supabase Admin API to live-test the onboarding popup past Cloudflare Turnstile (see `01 - Daily Notes/2026-08-30.md`, Session 10). That session explicitly left "delete the test account via the Supabase Dashboard" as Werner's action; it has not been done, so the row is still live.

**It is not filtered.** Verified directly: `isInternalTestAccount('lazyrelay-disposable-onboarding-test@example.com')` returns **`false`**. `@example.com` is reserved by RFC 2606 exactly like `@lazyrelay.invalid`, but only `.invalid` is in the domain-pattern list — `example.com` was never added. So this row reads as a genuine external signup to both candidate queries.

**Deadline: 2026-09-06.** At 7 days old with zero connected social accounts it lands on the stuck-onboarding list, and `lazyrelay-accounts-ops-daily` (which holds real send authority) emails it Template 11 from `support@lazyrelay.com`. The address is undeliverable, so the practical damage is a bounce rather than a customer-visible mistake — but it would still be a real unattended send to a non-customer, and a bounce off `support@` is its own small deliverability cost.

This is the **fourth instance of the same class** (Google reviewer `lazyrelay+reviewer@`, Werner's `+lrtest1`, the `lazyrelay@gmail.com` login change, now this) — each one a fixture that reached production data and slipped the filter. The `stripGmailAlias()` fix closed the plus-tag variant; this one is a *domain* gap, which that fix doesn't cover. **Two ways to close it (both need Werner — one is a live-data delete, the other a source-code edit, neither inside this task's authority):** delete the `accounts` row (also clears the `auth.users` row, matching the Session 10 plan), or add `@example.com` to the domain-pattern list in `ops/shared/internalTestAccounts.js`. Adding the domain pattern is the more durable of the two, since `example.com`/`example.org`/`example.net` are the standard reserved names any future test fixture is likely to reuse — but it does not remove the live row, so doing both is the complete fix.

**FILTER HALF RESOLVED same day (2026-08-31), on Werner's explicit go-ahead — the 09-06 send risk is closed.** `INTERNAL_TEST_EMAIL_PATTERNS` now carries `/@(?:[^@]+\.)?example\.(?:com|net|org)$/i` and `/@(?:[^@]+\.)?example$/i` (the reserved `.example` TLD from the same RFC 2606 clause, added on the same rationale). Widening a *pattern* rather than adding another exact address is safe here in a way it is not for `@gmail.com`: these domains are reserved precisely so they can never be registered or receive mail, so no real customer can ever collide with them. The exact-list comment's "never widen the domain patterns above" rule was clarified in place to say it governs *deliverable* domains only, so this doesn't read as a violation later.

Verified, not assumed — 27/27 explicit cases pass, covering the fixture itself, all four reserved-domain forms plus subdomains, every pre-existing pattern and exact entry, the `stripGmailAlias()` path, and negatives that must **not** match (`examples.com`, `example.com.co`, `myexample.com`, `notexample.org`, `exampletest.com`, `lazyrelay.invalid.com`, an ordinary customer domain, and null/undefined/empty). Then confirmed end-to-end against the live database: **genuine-external count went 1 → 0**, and both candidate lists stay empty. Both auditor self-tests still `overallPass: true`.

One thing that deliberately did **not** change: `accounts_auditor.js` still reports **`checked: 2`**. The auditor counts raw non-cancelled `accounts` rows and applies no internal-account filtering — only the candidate queries filter. A future run seeing `checked: 2` is correct and is *not* evidence the filter regressed; the number to watch is the genuine-external/candidate count, which is 0.

**Confirmed on the sending path the same day (2026-08-31).** `lazyrelay-accounts-ops-daily` — the task that actually holds send authority — ran independently after the filter fix: self-test `overallPass: true` (5/5), auditor `checked: 2 / flaggedCount: 0`, both candidate lists empty, nothing sent, and `isInternalTestAccount('lazyrelay-disposable-onboarding-test@example.com') === true` re-verified live. So the 09-06 risk is closed on the sending path, not just in the test harness. `checked: 2` is the current baseline — carry this date forward on quiet runs rather than adding a paragraph.

~~**The delete half is still open and still Werner's**~~ — **CLOSED, confirmed 2026-09-03.** The `lazyrelay-disposable-onboarding-test@example.com` row is gone from `accounts` (only `shop@lazydownloader.co.za` and `lazyrelay@gmail.com` remain). Both halves of the 08-31 item are now done: the filter pattern stays in place (still returns `true` for that address, harmless now that no row matches), and the live row has been deleted. Nothing outstanding here.

**2026-09-01 — baseline moved again: `checked: 3`, and the third row carries no send risk.** Auditor `checked: 3 / flaggedCount: 0`; both candidate lists empty; nothing sent. The new row is **`lazyrelay@gmail.com`** (created 2026-08-31 10:37 UTC) — the login address Werner moved the dogfooding account to on 2026-08-29, now appearing as an `accounts` row in its own right. Unlike the four prior fixture-slips, this one was **already covered before it existed**: the address was added to `INTERNAL_TEST_EMAILS_EXACT` on 2026-08-30 (commit `c658ba3`), so `isInternalTestAccount('lazyrelay@gmail.com')` returned `true` the moment the row appeared. Verified live this run, along with the negative control (`a.real.customer@somebusiness.com` → `false`), so the filter is covering it without having been widened too far. All three rows now read internal; genuine-external count is **0**.

~~`checked: 3` is the current baseline.~~ **2026-09-03 — the baseline is back down to `checked: 2`.** The disposable `@example.com` fixture was deleted (see the 08-31 block above), and `lazyrelay@gmail.com` now shows `created_at: 2026-09-02 13:18 UTC` rather than the 08-31 timestamp recorded here, so that row was itself removed and re-created in the same clean-up. The two surviving rows are `shop@lazydownloader.co.za` (2026-07-27) and `lazyrelay@gmail.com` (2026-09-02); both re-verified `isInternalTestAccount() === true` live this run, with the negative control (`a.real.customer@somebusiness.com` → `false`) still correct. Genuine-external count **0**, both candidate lists empty, auditor `flaggedCount: 0`.

`checked: 2` is the current baseline. Same caveat as the 08-31 entry, and note it now cuts both ways: the auditor counts raw non-cancelled rows with no internal filtering, so `checked` moving in *either* direction is population churn, not by itself evidence of a regression — the number to watch is the genuine-external/candidate count, still 0. Carry this date forward on quiet runs. **Still holding 2026-09-04** (read-only digest): self-test 5/5, `checked: 2 / flaggedCount: 0`, both candidate lists empty, review-feedback `count: 0`, nothing posted.

**Confirmed on the sending path the same day (2026-09-01).** `lazyrelay-accounts-ops-daily` — the task that actually holds send authority — ran independently after the read-only digest: self-test `overallPass: true` (5/5), auditor `checked: 3 / flaggedCount: 0`, both candidate lists empty, nothing sent. All three rows re-verified `isInternalTestAccount() === true` live; genuine-external count **0**. So the `checked: 3` baseline holds on the sending path, not just the reporting one. **Baseline still holding on the sending path as of 2026-09-02** — identical figures (self-test 5/5, `checked: 3 / flaggedCount: 0`, both lists empty, nothing sent, all three rows internal, genuine-external 0). **2026-09-03 confirmed on the sending path** at the new lower baseline: self-test 5/5, auditor `checked: 2 / flaggedCount: 0`, both candidate lists empty, nothing sent, both surviving rows (`shop@lazydownloader.co.za`, `lazyrelay@gmail.com`) `isInternalTestAccount() === true` live, negative control `false`, genuine-external **0**. **2026-09-04 confirmed on the sending path**, identical figures: self-test 5/5 `overallPass: true`, auditor `checked: 2 / flaggedCount: 0`, both candidate lists empty, nothing sent, both surviving rows (`shop@lazydownloader.co.za` 2026-07-27, `lazyrelay@gmail.com` 2026-09-02) `isInternalTestAccount() === true` live, negative control `false`, genuine-external **0**.

One caveat worth carrying: a fixture created on a genuinely registrable throwaway domain would still slip through, since no pattern can cover that class. The durable habit is to create test fixtures on `@lazyrelay.invalid` or `@example.com` — both now filtered — rather than relying on the filter to recognise an arbitrary address after the fact.

## Standing rule

If an account is in a state this file doesn't cover, don't improvise a fix — flag it in the run report for the user, same standing rule as the email agent's SUPPORT_KNOWLEDGE.md.
